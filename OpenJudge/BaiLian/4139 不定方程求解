#include<stdio.h>

int main()
{
	int a,b,c;
	scanf("%d %d %d",&a,&b,&c);
	int x,y;
	int sum=0;
	for(x=0;x<=1000;x++)
	{
		for(y=0;y<=1000;y++)
		{
			if(a*x+b*y==c)
			{
				sum++;
			}
		}
	}
	printf("%d",sum);
	return 0;
}
